# Weekly Business Signal Brief

The Weekly Business Signal Brief is a deterministic decision-support engine designed for appointment-based service businesses.

It converts structured weekly operational data (marketing, bookings, revenue) into measurable, rule-based business signals. All analytical logic is deterministic and auditable. AI is used strictly as a language rendering layer — never as a decision engine.
